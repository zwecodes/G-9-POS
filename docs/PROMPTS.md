<!-- Keep as a low-priority scratch log if you're tracking prompts you've given an AI coding agent — but treat it as non-authoritative. Nothing in it should ever be the source of truth for a decision; that's what the per-doc "Resolved Decisions" tables are for.Keep as a low-priority scratch log if you're tracking prompts you've given an AI coding agent — but treat it as non-authoritative. Nothing in it should ever be the source of truth for a decision; that's what the per-doc "Resolved Decisions" tables are for. -->